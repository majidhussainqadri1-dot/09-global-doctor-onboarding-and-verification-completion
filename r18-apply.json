{
  "round": "R09",
  "replacements": [
    {
      "path": "includes/class-gdo-membership-adapter.php",
      "count": 1,
      "old": "\tpublic static function identity_assurance_current( $user_id ) {\n\t\t$base = self::base_assertion( $user_id );\n\t\treturn $base\n\t\t\t&& ! empty( $base['application_exists'] )\n\t\t\t&& 'approved' === sanitize_key( $base['status'] )\n\t\t\t&& ! empty( $base['approved'] )\n\t\t\t&& ! empty( $base['identity_documents_current'] )\n\t\t\t&& ! empty( $base['email_verified'] )\n\t\t\t&& ! empty( $base['phone_verified'] )\n\t\t\t&& ! empty( $base['two_factor_ready'] );\n\t}",
      "new": "\tpublic static function identity_assurance_current_checked( $user_id ) {\n\t\t$user_id = absint( $user_id );\n\t\tif ( ! self::available() || ! $user_id ) {\n\t\t\treturn new WP_Error( 'gdo_identity_dependency_unavailable', __( 'Current File 00 identity assurance is temporarily unavailable.', 'global-doctor-onboarding' ) );\n\t\t}\n\t\ttry {\n\t\t\t$base = SMC_Contracts::assertions( $user_id );\n\t\t} catch ( Throwable $e ) {\n\t\t\tunset( $e );\n\t\t\treturn new WP_Error( 'gdo_identity_assertion_unavailable', __( 'Current File 00 identity assurance could not be read safely.', 'global-doctor-onboarding' ) );\n\t\t}\n\t\tif ( ! self::valid_base_assertion( $base, $user_id ) ) {\n\t\t\treturn new WP_Error( 'gdo_identity_assertion_invalid', __( 'Current File 00 identity assurance returned an invalid contract response.', 'global-doctor-onboarding' ) );\n\t\t}\n\t\treturn ! empty( $base['application_exists'] )\n\t\t\t&& 'approved' === sanitize_key( $base['status'] )\n\t\t\t&& ! empty( $base['approved'] )\n\t\t\t&& ! empty( $base['identity_documents_current'] )\n\t\t\t&& ! empty( $base['email_verified'] )\n\t\t\t&& ! empty( $base['phone_verified'] )\n\t\t\t&& ! empty( $base['two_factor_ready'] );\n\t}\n\n\tpublic static function identity_assurance_current( $user_id ) {\n\t\t$checked = self::identity_assurance_current_checked( $user_id );\n\t\treturn is_wp_error( $checked ) ? false : (bool) $checked;\n\t}"
    },
    {
      "path": "includes/class-gdo-advanced-trust.php",
      "count": 1,
      "old": "        $identity = GDO_Membership_Adapter::identity_assurance_current( $app->user_id );\n        $matrix['identity'] = (bool) $identity;",
      "new": "        $identity = GDO_Membership_Adapter::identity_assurance_current_checked( $app->user_id );\n        if ( is_wp_error( $identity ) ) {\n            $matrix['current_status'] = false;\n            $matrix['data_available'] = false;\n            return $matrix;\n        }\n        $matrix['identity'] = (bool) $identity;"
    },
    {
      "path": "includes/class-gdo-advanced-trust.php",
      "count": 1,
      "old": "        $verified_expiry=self::current_verification_expiry($app);$approved_snapshot=$app?GDO_Application::stored_approved_snapshot($app):array();if(!$app||!$approved_snapshot||empty($approved_snapshot['captured_at'])||!GDO_State::public_verified($app->state)||!GDO_Membership_Adapter::identity_assurance_current($app->user_id)||!$verified_expiry){$wpdb->query('ROLLBACK');return new WP_Error('gdo_passport_not_eligible',__('A current verified application, intact approved snapshot, explicit future validity date, and current identity assurance are required for a professional passport.','global-doctor-onboarding'));}",
      "new": "        $verified_expiry=self::current_verification_expiry($app);$approved_snapshot=$app?GDO_Application::stored_approved_snapshot($app):array();$identity=$app?GDO_Membership_Adapter::identity_assurance_current_checked($app->user_id):false;if(is_wp_error($identity)){$wpdb->query('ROLLBACK');return new WP_Error('gdo_passport_identity_unavailable',__('Current identity assurance is temporarily unavailable for professional passport issuance.','global-doctor-onboarding'),array('status'=>503));}if(!$app||!$approved_snapshot||empty($approved_snapshot['captured_at'])||!GDO_State::public_verified($app->state)||!$identity||!$verified_expiry){$wpdb->query('ROLLBACK');return new WP_Error('gdo_passport_not_eligible',__('A current verified application, intact approved snapshot, explicit future validity date, and current identity assurance are required for a professional passport.','global-doctor-onboarding'));}"
    },
    {
      "path": "includes/class-gdo-advanced-trust.php",
      "count": 1,
      "old": "        $verified_expiry=self::current_verification_expiry($app);$approved_snapshot=$app?GDO_Application::stored_approved_snapshot($app):array();$passport_issued=!empty($row['issued_at'])?strtotime($row['issued_at'].' UTC'):0;$snapshot_captured=!empty($approved_snapshot['captured_at'])?strtotime($approved_snapshot['captured_at'].' UTC'):0;if(!$app||!$approved_snapshot||!$passport_issued||!$snapshot_captured||$passport_issued<$snapshot_captured||absint($app->user_id)!==absint($row['user_id'])||!GDO_State::public_verified($app->state)||!GDO_Membership_Adapter::identity_assurance_current($row['user_id'])||!$verified_expiry){",
      "new": "        $verified_expiry=self::current_verification_expiry($app);$approved_snapshot=$app?GDO_Application::stored_approved_snapshot($app):array();$passport_issued=!empty($row['issued_at'])?strtotime($row['issued_at'].' UTC'):0;$snapshot_captured=!empty($approved_snapshot['captured_at'])?strtotime($approved_snapshot['captured_at'].' UTC'):0;$identity=GDO_Membership_Adapter::identity_assurance_current_checked($row['user_id']);if(is_wp_error($identity)){return new WP_Error('gdo_passport_identity_unavailable',__('Current identity assurance is temporarily unavailable.','global-doctor-onboarding'),array('status'=>503));}if(!$app||!$approved_snapshot||!$passport_issued||!$snapshot_captured||$passport_issued<$snapshot_captured||absint($app->user_id)!==absint($row['user_id'])||!GDO_State::public_verified($app->state)||!$identity||!$verified_expiry){"
    },
    {
      "path": "includes/class-gdo-advanced-trust-hardening.php",
      "count": 1,
      "old": "        $verified_expiry = GDO_Advanced_Trust::current_verification_expiry( $app );\n        $approved_snapshot = $app ? GDO_Application::stored_approved_snapshot( $app ) : array();\n        if ( ! $app || ! $approved_snapshot || empty( $approved_snapshot['captured_at'] ) || ! GDO_State::public_verified( $app->state ) || 'accepted' !== sanitize_key( $app->claim_status ) || ! GDO_Membership_Adapter::identity_assurance_current( $app->user_id ) || ! $verified_expiry ) {",
      "new": "        $verified_expiry = GDO_Advanced_Trust::current_verification_expiry( $app );\n        $approved_snapshot = $app ? GDO_Application::stored_approved_snapshot( $app ) : array();\n        $identity = $app ? GDO_Membership_Adapter::identity_assurance_current_checked( $app->user_id ) : false;\n        if ( is_wp_error( $identity ) ) { $wpdb->query( 'ROLLBACK' ); return new WP_Error( 'gdo_passport_identity_unavailable', __( 'Current identity assurance is temporarily unavailable for professional passport issuance.', 'global-doctor-onboarding' ), array( 'status'=>503 ) ); }\n        if ( ! $app || ! $approved_snapshot || empty( $approved_snapshot['captured_at'] ) || ! GDO_State::public_verified( $app->state ) || 'accepted' !== sanitize_key( $app->claim_status ) || ! $identity || ! $verified_expiry ) {"
    },
    {
      "path": "includes/class-gdo-advanced-trust-hardening.php",
      "count": 1,
      "old": "        $verified_expiry=GDO_Advanced_Trust::current_verification_expiry($app);$approved_snapshot=$app?GDO_Application::stored_approved_snapshot($app):array();$passport_issued=!empty($row['issued_at'])?strtotime($row['issued_at'].' UTC'):0;$snapshot_captured=!empty($approved_snapshot['captured_at'])?strtotime($approved_snapshot['captured_at'].' UTC'):0;\n        if(!$app||!$approved_snapshot||!$passport_issued||!$snapshot_captured||$passport_issued<$snapshot_captured||absint($app->user_id)!==absint($row['user_id'])||!GDO_State::public_verified($app->state)||'accepted'!==sanitize_key($app->claim_status)||!GDO_Membership_Adapter::identity_assurance_current($row['user_id'])||!$verified_expiry){return new WP_Error('gdo_passport_inactive',__('This professional verification passport is not active.','global-doctor-onboarding'),array('status'=>404));}",
      "new": "        $verified_expiry=GDO_Advanced_Trust::current_verification_expiry($app);$approved_snapshot=$app?GDO_Application::stored_approved_snapshot($app):array();$passport_issued=!empty($row['issued_at'])?strtotime($row['issued_at'].' UTC'):0;$snapshot_captured=!empty($approved_snapshot['captured_at'])?strtotime($approved_snapshot['captured_at'].' UTC'):0;$identity=GDO_Membership_Adapter::identity_assurance_current_checked($row['user_id']);if(is_wp_error($identity)){return new WP_Error('gdo_passport_identity_unavailable',__('Current identity assurance is temporarily unavailable.','global-doctor-onboarding'),array('status'=>503));}\n        if(!$app||!$approved_snapshot||!$passport_issued||!$snapshot_captured||$passport_issued<$snapshot_captured||absint($app->user_id)!==absint($row['user_id'])||!GDO_State::public_verified($app->state)||'accepted'!==sanitize_key($app->claim_status)||!$identity||!$verified_expiry){return new WP_Error('gdo_passport_inactive',__('This professional verification passport is not active.','global-doctor-onboarding'),array('status'=>404));}"
    }
  ]
}
