name: RC2 Source Export

on:
  push:
    branches:
      - codex/file09-1.2.0-rc2-completion
  workflow_dispatch:

permissions:
  contents: read

jobs:
  export:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout exact branch head
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Record exact head
        run: |
          git rev-parse HEAD > EXACT-HEAD.txt
          find . -path './.git' -prune -o -type f -print0 | sort -z | xargs -0 sha256sum > EXACT-HEAD-SOURCE-CHECKSUMS.sha256
      - name: Build source archive
        run: |
          tar --exclude=.git -czf file09-rc2-source.tar.gz .
      - name: Upload source archive
        uses: actions/upload-artifact@v4
        with:
          name: file09-rc2-source
          path: file09-rc2-source.tar.gz
          if-no-files-found: error
          retention-days: 1
