{
  "replacements": [
    {
      "path": "includes/class-gdo-advanced-trust.php",
      "old": "        if(1!==$inserted||false===$wpdb->query('COMMIT')){$wpdb->query('ROLLBACK');@unlink($path);return new WP_Error('gdo_upload_session_store',__('The resumable upload session could not be recorded.','global-doctor-onboarding'));}\n        return array('upload_uuid'=>$uuid,'expires_at'=>gmdate('c',time()+self::CHUNK_TTL));",
      "new": "        if(1!==$inserted){$wpdb->query('ROLLBACK');$deleted=!is_file($path)||(@unlink($path)&&!file_exists($path));if(!$deleted){GDO_Membership_Adapter::audit('doctor_resumable_upload_orphan_cleanup_failed',array('upload_uuid'=>$uuid,'phase'=>'session_insert'));return new WP_Error('gdo_upload_session_orphan_cleanup',__('The resumable upload session could not be recorded and its private temporary file requires operator cleanup.','global-doctor-onboarding'));}return new WP_Error('gdo_upload_session_store',__('The resumable upload session could not be recorded.','global-doctor-onboarding'));}\n        if(false===$wpdb->query('COMMIT')){\n            $wpdb->query('ROLLBACK');\n            $wpdb->last_error='';\n            $persisted=$wpdb->get_row($wpdb->prepare(\"SELECT upload_uuid,expires_at,state FROM {$sessions} WHERE upload_uuid=%s AND user_id=%d LIMIT 1\",$uuid,$uid));\n            if(null===$persisted&&!empty($wpdb->last_error)){GDO_Membership_Adapter::audit('doctor_resumable_upload_commit_uncertain',array('upload_uuid'=>$uuid,'phase'=>'session_create'));return new WP_Error('gdo_upload_session_commit_uncertain',__('The resumable upload session commit status is temporarily uncertain. Its private file was retained for safe reconciliation.','global-doctor-onboarding'));}\n            if($persisted){return array('upload_uuid'=>$uuid,'expires_at'=>gmdate('c',strtotime($persisted->expires_at.' UTC')),'commit_recovered'=>true);}\n            $deleted=!is_file($path)||(@unlink($path)&&!file_exists($path));if(!$deleted){GDO_Membership_Adapter::audit('doctor_resumable_upload_orphan_cleanup_failed',array('upload_uuid'=>$uuid,'phase'=>'session_commit'));return new WP_Error('gdo_upload_session_orphan_cleanup',__('The resumable upload session was not committed and its private temporary file requires operator cleanup.','global-doctor-onboarding'));}\n            return new WP_Error('gdo_upload_session_store',__('The resumable upload session could not be committed.','global-doctor-onboarding'));\n        }\n        return array('upload_uuid'=>$uuid,'expires_at'=>gmdate('c',time()+self::CHUNK_TTL));"
    },
    {
      "path": "includes/class-gdo-advanced-trust.php",
      "old": "        if(1!==$updated||false===$wpdb->query('COMMIT')){$truncate_ok=ftruncate($fh,$before);if($truncate_ok&&function_exists('fsync')){$truncate_ok=fsync($fh);}flock($fh,LOCK_UN);fclose($fh);$wpdb->query('ROLLBACK');if(!$truncate_ok){GDO_Membership_Adapter::audit('doctor_resumable_upload_rollback_file_failed',array('upload_uuid'=>$row->upload_uuid,'expected_bytes'=>$before));return new WP_Error('gdo_upload_chunk_rollback_file',__('The uncommitted chunk could not be rolled back safely; operator repair is required.','global-doctor-onboarding'));}return new WP_Error('gdo_upload_chunk_conflict',__('The upload chunk could not be committed safely.','global-doctor-onboarding'));}\n        flock($fh,LOCK_UN);fclose($fh);return array('received_chunks'=>absint($row->received_chunks)+1,'received_bytes'=>$before+$written);",
      "new": "        if(1!==$updated){$truncate_ok=ftruncate($fh,$before);if($truncate_ok&&function_exists('fsync')){$truncate_ok=fsync($fh);}flock($fh,LOCK_UN);fclose($fh);$wpdb->query('ROLLBACK');if(!$truncate_ok){GDO_Membership_Adapter::audit('doctor_resumable_upload_rollback_file_failed',array('upload_uuid'=>$row->upload_uuid,'expected_bytes'=>$before));return new WP_Error('gdo_upload_chunk_rollback_file',__('The uncommitted chunk could not be rolled back safely; operator repair is required.','global-doctor-onboarding'));}return new WP_Error('gdo_upload_chunk_conflict',__('The upload chunk could not be recorded safely.','global-doctor-onboarding'));}\n        if(false===$wpdb->query('COMMIT')){\n            $wpdb->query('ROLLBACK');\n            $wpdb->last_error='';\n            $persisted=$wpdb->get_row($wpdb->prepare(\"SELECT received_bytes,received_chunks,state FROM {$table} WHERE upload_uuid=%s AND user_id=%d LIMIT 1\",$row->upload_uuid,$uid));\n            if(null===$persisted&&!empty($wpdb->last_error)){flock($fh,LOCK_UN);fclose($fh);GDO_Membership_Adapter::audit('doctor_resumable_upload_commit_uncertain',array('upload_uuid'=>$row->upload_uuid,'phase'=>'chunk','chunk'=>$index));return new WP_Error('gdo_upload_chunk_commit_uncertain',__('The upload chunk commit status is temporarily uncertain. File bytes were retained for safe reconciliation.','global-doctor-onboarding'));}\n            if($persisted&&absint($persisted->received_bytes)===$before+$written&&absint($persisted->received_chunks)===absint($row->received_chunks)+1){flock($fh,LOCK_UN);fclose($fh);return array('received_chunks'=>absint($persisted->received_chunks),'received_bytes'=>absint($persisted->received_bytes),'commit_recovered'=>true);}\n            $truncate_ok=ftruncate($fh,$before);if($truncate_ok&&function_exists('fsync')){$truncate_ok=fsync($fh);}flock($fh,LOCK_UN);fclose($fh);if(!$truncate_ok){GDO_Membership_Adapter::audit('doctor_resumable_upload_rollback_file_failed',array('upload_uuid'=>$row->upload_uuid,'expected_bytes'=>$before));return new WP_Error('gdo_upload_chunk_rollback_file',__('The non-committed chunk could not be rolled back safely; operator repair is required.','global-doctor-onboarding'));}\n            return new WP_Error('gdo_upload_chunk_conflict',__('The upload chunk was not committed safely.','global-doctor-onboarding'));\n        }\n        flock($fh,LOCK_UN);fclose($fh);return array('received_chunks'=>absint($row->received_chunks)+1,'received_bytes'=>$before+$written);"
    },
    {
      "path": "includes/class-gdo-advanced-trust.php",
      "old": "        $claimed=$wpdb->update($table,array('state'=>'finalizing','updated_at'=>self::now()),array('upload_uuid'=>$row->upload_uuid,'state'=>'open'),array('%s','%s'),array('%s','%s'));\n        if(1!==$claimed||false===$wpdb->query('COMMIT')){$wpdb->query('ROLLBACK');return new WP_Error('gdo_upload_finalize_conflict',__('The resumable upload is already being finalized.','global-doctor-onboarding'));}\n        $health=GDO_Storage::health();",
      "new": "        $claimed=$wpdb->update($table,array('state'=>'finalizing','updated_at'=>self::now()),array('upload_uuid'=>$row->upload_uuid,'state'=>'open'),array('%s','%s'),array('%s','%s'));\n        if(1!==$claimed){$wpdb->query('ROLLBACK');return new WP_Error('gdo_upload_finalize_conflict',__('The resumable upload is already being finalized.','global-doctor-onboarding'));}\n        if(false===$wpdb->query('COMMIT')){\n            $wpdb->query('ROLLBACK');$wpdb->last_error='';\n            $persisted_state=$wpdb->get_var($wpdb->prepare(\"SELECT state FROM {$table} WHERE upload_uuid=%s AND user_id=%d LIMIT 1\",$row->upload_uuid,$uid));\n            if(null===$persisted_state&&!empty($wpdb->last_error)){GDO_Membership_Adapter::audit('doctor_resumable_upload_commit_uncertain',array('upload_uuid'=>$row->upload_uuid,'phase'=>'finalize_claim'));return new WP_Error('gdo_upload_finalize_commit_uncertain',__('The upload finalization claim is temporarily uncertain; retry after database recovery.','global-doctor-onboarding'));}\n            if('finalizing'!==$persisted_state){return new WP_Error('gdo_upload_finalize_conflict',__('The resumable upload finalization claim was not committed safely.','global-doctor-onboarding'));}\n        }\n        $health=GDO_Storage::health();"
    }
  ]
}
