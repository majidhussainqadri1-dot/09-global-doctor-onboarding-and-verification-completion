{
  "message": "fix(file09): keep privacy eraser retryable until selected applications finish",
  "changes": [
    {
      "path": "includes/class-gdo-privacy.php",
      "old": "\t\t$removed = false;\n\t\t$retained = $held > 0;",
      "new": "\t\t$removed = false;\n\t\t$completed_apps = 0;\n\t\t$retained = $held > 0;"
    },
    {
      "path": "includes/class-gdo-privacy.php",
      "old": "\t\t\t$removed = true;\n\t\t\t$retained = true;\n\t\t\t$messages[] = 'Personal credential and Advanced Trust data were erased or anonymized; minimal accountability evidence was retained.';",
      "new": "\t\t\t$removed = true;\n\t\t\t++$completed_apps;\n\t\t\t$retained = true;\n\t\t\t$messages[] = 'Personal credential and Advanced Trust data were erased or anonymized; minimal accountability evidence was retained.';"
    },
    {
      "path": "includes/class-gdo-privacy.php",
      "old": "\t\treturn array( 'items_removed'=>$removed, 'items_retained'=>$retained, 'messages'=>array_values( array_unique( $messages ) ), 'done'=>count( $apps ) < $limit );",
      "new": "\t\treturn array( 'items_removed'=>$removed, 'items_retained'=>$retained, 'messages'=>array_values( array_unique( $messages ) ), 'done'=>count( $apps ) < $limit && $completed_apps === count( $apps ) );"
    }
  ]
}
